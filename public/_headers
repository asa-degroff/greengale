# Enable SharedArrayBuffer for WASM multi-threading (@jsquash/avif encoder)
# These headers must be on ALL pages because SPA client-side routing doesn't trigger new document requests
# Use 'credentialless' instead of 'require-corp' to allow loading external images (e.g., user avatars from CDN)

/*
  Cross-Origin-Embedder-Policy: credentialless
  Cross-Origin-Opener-Policy: same-origin
  Cross-Origin-Resource-Policy: same-origin
